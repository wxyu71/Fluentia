version: "2"

linters:
  default: standard
  enable:
    - gocritic

  settings:
    gocritic:
      enabled-tags:
        - diagnostic
        - performance

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
